作用：根据版本号判断release的installer和branch
步骤：
1.Put the two files git.cmd and git.jar in the server installshield-dev :

2.Double click the file git.cmd and input the version number:

3.It will send the email
Note:必须将文件放在installshield-dev上面使用，存放目录可以任意